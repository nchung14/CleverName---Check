// Nolan Chungs
// AI to play chess on Jaz's CleverName-Check

/* Classes
 * 
 * Piece(type, value, coordinate, danger, potential moves)
 * 
 * */

import info.gridworld.grid.Grid;
import info.gridworld.actor.Actor;
import info.gridworld.actor.Bug;
import java.util.ArrayList;
import info.gridworld.grid.Location;

public class CleverNameCheckAI
{
  ArrayList<Location> possibleMoveActors = getGrid().getNeighbors(getLocation());
  for(int i = 0; i < possibleMoveActors.size() - 1; i++)
  {
    if(possibleMoveActors.get(i) instanceof WhiteKing || instanceof WhitePiece)
    {
      possibleMoveActors
    }
  }
}